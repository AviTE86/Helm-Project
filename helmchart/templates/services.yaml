apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  labels:
spec:
 ### ADD IF ENV=LOCAL > TYPE:NODEPORT ELSE > 
  type: LoadBalancer
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
  selector:
    app: {{ .Release.Name }}

